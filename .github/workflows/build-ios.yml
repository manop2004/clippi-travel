name: Build iOS App (Cloud Build)

on:
  push:
    branches: [ main, master, backend-devnon ]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-14

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install --legacy-peer-deps

      - name: Build Web Production Assets
        run: npm run build

      - name: Add & Sync Capacitor iOS (Fresh)
        run: |
          rm -rf ios
          npx cap add ios
          npx cap sync ios

      - name: Build Xcode iOS App
        run: |
          cd ios/App
          if [ -d "App.xcworkspace" ]; then
            XCODE_TARGET="-workspace App.xcworkspace"
          else
            XCODE_TARGET="-project App.xcodeproj"
          fi

          xcodebuild $XCODE_TARGET \
                     -scheme App \
                     -configuration Release \
                     -sdk iphonesimulator \
                     -derivedDataPath build \
                     CODE_SIGNING_ALLOWED=NO \
                     CODE_SIGNING_REQUIRED=NO \
                     CODE_SIGN_IDENTITY="" \
                     build

      - name: Create IPA & App Zip Artifacts
        run: |
          rm -rf Payload Clippi-iOS-App.ipa Clippi-iOS-App.zip
          mkdir -p Payload
          if [ -d "ios/App/build/Build/Products/Release-iphoneos/App.app" ]; then
            cp -r ios/App/build/Build/Products/Release-iphoneos/App.app Payload/
          elif [ -d "ios/App/build/Build/Products/Release-iphonesimulator/App.app" ]; then
            cp -r ios/App/build/Build/Products/Release-iphonesimulator/App.app Payload/
          fi
          zip -r Clippi-iOS-App.ipa Payload
          zip -r Clippi-iOS-App.zip Payload

      - name: Upload iOS App Artifact (.ipa)
        uses: actions/upload-artifact@v4
        with:
          name: clippi-ios-app-ipa
          path: Clippi-iOS-App.ipa

      - name: Upload iOS App Artifact (.zip)
        uses: actions/upload-artifact@v4
        with:
          name: clippi-ios-app-zip
          path: Clippi-iOS-App.zip

